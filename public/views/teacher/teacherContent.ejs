<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title><%= info.title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="../../css/teacher/materialList.css">
</head>

<body>
    <% include searchHeader %>

    <%
    //Verificacao se ha ou nao materiais cadastrados
    function verifyMaterial(materials) {
        if(materials.length == 0){
            return false;
        }else{
            return true;
        }
    };

    //Verificar o tipo de imagem
    function getImage(subject) {
        switch (subject) {
            case "Operações Básicas":
                return "more";
            case "Fração":
                return "fraction";
            case "Álgebra":
                return "algebra"
            case "Análise Combinatória":
                return "division";
            case "Geometria":
                return "area";
            case "Porcentagem":
                return "percent";
            default:
                return "other";
        }
    }
    %>

    <main class="adicionar-material">
        <section class="line">
            <a href="/teacher/content/create"> <img class="img-cadastrar" src="../../img/img-guia-estudo/more.png" alt="">
            </a>
        </section>
        <section class="line">
            <%
            var myContentsTitle;
            var image;
            if(verifyMaterial(myContents)){
                myContentsTitle = "Meus Conteúdos";
            }else{
                myContentsTitle = "Voce ainda nao cadastrou nenhum conteúdo"
            };
            %>
            <h2 class="center"><%= myContentsTitle %></h2>
            <div class="lista">
                <% myContents.forEach( element => { %>
                <div class="img-configuration">
                    <%
                    image = getImage(element.subject);
                    %>
                        <a href="/teacher/content/delete/<%= element.id %>"><img class="img-delete" src="../../img/img-menu/delete.png"  alt=""> </a>
                    <a href="/teacher/content/view/<%= element.id %>"> <img class="img-subject" src="../../img/img-conteudo/<%= image %>.png" alt=""> </a>
                    <h3><%= element.name %></h3>
                    <span class="span-nome"><%= element.author %></span>
                </div>
                <% }); %>
            </div>

        </section>

        <section>
            <h2 class="center">Todos os Conteúdos</h2>
            <div class="lista">
                <% contents.forEach( element => { %>
                <div class="img-configuration">
                    <%
                    image = getImage(element.subject);
                    %>
                    <a href="/teacher/content/view/<%= element.id %>"> <img class="img-subject" src="../../img/img-conteudo/<%= image %>.png" alt=""> </a>
                    <h3><%= element.name %></h3>
                    <span class="span-nome"><%= element.author %></span>
                </div>
                <% }); %>
            </div>
        </section>
    </main>
</body>

</html>